# 原价字段接入：中断交接

状态：代码已修改，验证及文档维护未完成，尚未提交或推送。
日期：2026-09-08
环境：work（公司电脑）
暂停原因：用户额度将尽，要求记录未完成内容后停止。

## 本轮需求

- 相册后台：商品详情、新增、编辑页面新增四语种划线价（原价）。
- 字段：marketAmount（简中）、marketAmountEnglish（英文）、marketAmountFan（繁中）、marketAmountJapanese（日文）。
- 相册小程序 photo-album、相册APP：星币管理页面用接口原价替换此前静态划线价。

## 仓库与进度

路径从 environments/work/projects.local.yaml 解析，不切换其他环境。
本轮三个项目均已先执行 git pull --ff-only，结果 Already up to date；当前工作分支均为 main。修改前及上一轮结束后 CodeGraph 均已同步，本轮新增修改完成后的索引还需同步。

| 项目 ID | 本轮起始提交 | 目前情况 |
| --- | --- | --- |
| album-admin | 58813ff | 表单、提交字段和检查脚本已修改，未提交 |
| album-miniapp | 96e4d2a | 原价解析、展示及测试已修改，未提交 |
| album-app | 5d5bb1e | 原价模型、展示及测试已修改，未提交 |

上一轮静态划线价、六位主价格展示、后台隐藏 Token 单价列已完成并推送，不要重复实现。上一轮后台已有 dist.zip 删除已随 58813ff 推送。Hub 公司映射迁移和上一轮交付记录已随 2d46720 推送。

## 已有工作区修改

### 相册后台

- src/views/commerce/goods/components/GoodsForm.vue
- src/views/commerce/goods/useGoodsForm.js
- scripts/check-goods-market-amount.mjs（新文件）

共用表单的每个语种行增加原价输入和币种说明；详情沿用只读模式。新增四字段默认值、校验和提交转换。当前实现允许空值以兼容旧商品；允许 0，输入范围 0 至 99999999，精度两位小数；空值提交 null。不强制原价大于售价。

检查脚本使用实际 composable 源码配合模拟 API/Router，验证新增/编辑负载、详情回填、只读模式、旧商品空值及数值校验，不写入后端。

### 相册小程序

- utils/token-api.js
- subpackages/token/index/index.js
- subpackages/token/index/index.wxml
- subpackages/token/index/index.wxss
- tests/token-pay.test.js

解析接口原价，移除静态 ￥0.01；有效值展示两位小数并沿用删除线，缺失或非法值留空且保留行占位，0 保留。主价格币种也使用商品 currencySymbol，缺失回落 ¥。原价不参与实际下单金额计算。新增对应归一测试。

### 相册APP

- lib/src/features/star/star_coin_api.dart
- lib/src/features/star/presentation/star_coin_page.dart
- test/star_purchase_test.dart

StarPackage 增加可空原价及展示文本；套餐请求按发起时语种解析原价。页面删除静态金额，继续使用删除线；原价整行按需缩放，无值保留空位。实际支付价格保持使用原有 price。新增四语种、零值、非法值、缺失值和六位金额测试。以上三个 Dart 文件均已 format 成功。

## 接口返回形态：待确认

曾询问用户客户端 getGoodsList 是按当前语种统一返回 marketAmount，还是一次返回四个原价字段，尚未收到答复。

当前实现兼容两种形态：当前语种专属字段存在时使用它；专属字段不存在时回落 marketAmount，视为服务端已按语种投影的值。专属字段明确为 null/空/非法时不跨语种回退，避免串币种。语种码 0/1 英文、2 简中、3 繁中、4 日文。数字或数值字符串均解析，0 有效；负数、非有限数、布尔及其他非法类型不展示。

恢复时应核实上述兼容方式与真实接口一致，尤其非中文原价与 currencySymbol 的对应关系；若用户明确客户端只返回 marketAmount，可收敛为实际契约。未请求或写入真实业务 API。

## 已确认的验证

- 小程序 node --test tests/token-pay.test.js：新增原价测试及既有支付用例通过。
- Dart format：模型、页面、测试三个文件通过。
- 本轮后台 npm run build 已启动，返回 session_id 52274，但暂停前未读取最终结果，不能写为通过。
- 后台字段检查脚本的首次运行因进程启动失败未执行；随后沙箱外重试工具调用被用户中断，结果未知。
- 本轮 APP 测试和静态分析安排在被中断的工具调用里，是否启动及完成均需重新核实，不得沿用上一轮的通过结果。
- 上一轮小程序全量测试已知有一项未修改的 BLE 图传测试失败：tests/img-transfer-adaptive.test.js，“cap 不应涨回配置上限（实际 50）”；上一轮 57 项中 56 通过。本轮只运行原价/支付相关测试，不要误报本轮全量通过。
- 未做真机、浏览器视觉或真实接口联调。

## 恢复后的待办

1. 重新读取 Hub 和目标项目的 AGENTS.md、AI_CONTEXT.md、文档索引及相关 Active 文档。按当前环境映射核实仓库。
2. 先检查工作区：以上未提交文件是本轮成果，不能覆盖、清理或把它们当无关文件排除。若恢复被视为新一轮，按用户的先 pull 规则处理当前分支；已有修改不得擅自 stash/reset。
3. 审阅三仓 diff，核实字段拼写、详情回填、提交类型及客户端当前语种/币种；检查测试是否覆盖实际请求到展示的数据链路。
4. 后台运行 node scripts/check-goods-market-amount.mjs 和 npm run build，必要时修复；核对新增、编辑、详情四语种原价展示。
5. APP 运行 flutter test test/star_purchase_test.dart、flutter analyze --no-pub lib/src/features/star。SDK 使用本机现有 Flutter，不升级 SDK。
6. 小程序按需要再次运行上述定向测试及 JS 语法检查；手工核对原价缺失、零值、999999.99、四语种、卡片选中与未选中状态。
7. 更新各项目自身的 Active 接口文档和本轮变更记录，并在各自 docs/README.md 建立索引。尤其替换上一轮“静态 ￥0.01、等待接口”的当前说明；上一轮历史记录保持历史事实。后台需同步四语种表单/接口契约及原价允许为空的处理。
8. 各项目 codegraph sync . 后 codegraph status .，运行 git diff --check，检查暂存范围。
9. 上文用户曾明确要求完成后 commit 和 push，未撤回；当前仅要求暂停。完成剩余工作后按当时有效指令交付，不能把未验证成果误报为已提交。本轮截至暂停没有新提交或 push。
10. Hub 只保留分发、结果摘要；详细技术维护回到各自项目。更新本交接状态或 iterations/current.md 时保留上一轮已完成记录。

## 本机临时文件

Hub .codex-tmp/ 中有本轮临时修改脚本，仅为操作过程，不应提交，也不要再次运行：多数脚本要求旧文本精确匹配，部分测试生成脚本曾因美元符号替换语义出错，已通过 fix-market-tests.mjs 修正，当前两个测试文件才是正确结果。继续工作请直接审阅并维护目标项目文件。
